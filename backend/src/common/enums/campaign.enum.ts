export enum CampaignType {
    FLEXIBLE = 'FLEXIBLE', // Nhận bao nhiêu giải ngân bấy nhiêu
    FIXED = 'FIXED',       // Phải đủ target mới giải ngân
}

export enum CampaignStatus {
    ACTIVE = 'Active',
    CLOSED = 'Closed',
    SUSPENDED = 'Suspended',
}